<UserControl Name="expanderControl"
    x:Class="UniversalHelpersDemo.Controls.Expander"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:UniversalHelpersDemo.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Name="HeaderGrid">
            <TextBlock x:Name="HeaderText" HorizontalAlignment="Left" Margin="0,0" TextWrapping="Wrap" Text="Expander Header" VerticalAlignment="Top"/>
            <Button Click="HideShowContent" x:Name="OpenButton" Content="Expand" HorizontalAlignment="Right" Margin="0,0,0,0" VerticalAlignment="Top"/>
        </Grid>
        <ContentPresenter  Grid.Row="1" Content="{Binding Path=ExpanderContent, ElementName=expanderControl}" Name="contentPresenter" Visibility="Collapsed" />
    </Grid>


</UserControl>
